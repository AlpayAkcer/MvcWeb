@using MvcWeb.Models.Entity
@model List<Role>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

@foreach (var item in Model)
{
    @item.RoleName
}
